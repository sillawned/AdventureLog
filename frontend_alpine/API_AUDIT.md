# AdventureLog Frontend ↔ FastAPI API Audit (2025-11-13)

## Scope
Audit compares all active frontend calls (Alpine layer) against implemented FastAPI endpoints after migration. Identifies mismatches, disabled legacy features, and future implementation targets.

## Summary
- Core CRUD (locations, collections, categories, notes, visits, activities, transportation, lodging, trails) aligned.
- World Travel endpoints (countries, regions, visited regions/cities) aligned.
- Import/Export path corrected from `/import-export/*` (legacy) to `/data/*` (FastAPI).
- Legacy features (search, calendar, profile management) had no backend endpoints; calls now stubbed with user feedback.

## Corrected Paths
| Feature | Frontend (before) | Backend (actual) | Action |
|---------|-------------------|------------------|--------|
| Data Export | `/import-export/export/json` | `/data/export/json` | Updated |
| Data Import | `/import-export/import/json` | `/data/import/json` | Updated |

## Disabled / Stubbed Features
| Feature | Previous Frontend Calls | Backend Status | Current Behavior |
|---------|-------------------------|----------------|------------------|
| Search | `/search/?query=…` | Not implemented | Stub shows message |
| Calendar Events | `/adventures/calendar/` | Not implemented | Stub shows message |
| Calendar ICS | `/adventures/calendar/download/` | Not implemented | Stub shows message |
| Profile View | `/users/{id}/` + stats/locations/collections | Not implemented | Stub shows message |
| Profile Update | `/auth/user/` (PATCH) | Not implemented | Stub shows message |
| Account Delete | `/auth/user/` (DELETE) | Not implemented | Stub shows message |

## Active Endpoint Groups (Verified)
- Locations: list, detail, create, update, delete, sub-resources (activities, transportation, lodging, trails, notes, visits).
- Collections: list, detail, create, update, delete, location summaries.
- Categories: list, create, update, delete.
- World Travel: countries, regions by country, visited regions, visited cities.
- Stats: aggregated counts (where implemented in FastAPI).
- Auth: register, token, me (logout behavior depends on token invalidation strategy).
- Data Import/Export: JSON import/export via `/api/data/*`.

## Frontend Adjustments Implemented
- `additional-features.js` entirely refactored: duplicated, corrupted legacy block removed; now exports clean `additionalFeaturesMethods` with stubs.
- Import/export functions call corrected endpoints; profile/calendar/search now non-destructive.
- Navigation helpers `viewWorldTravel` / `viewCollections` retained and functional.

## Risks / Follow-Up
| Area | Risk | Recommendation |
|------|------|----------------|
| Stubs left in production | Users may expect features | Hide UI controls or add "Coming Soon" badges |
| Auth profile endpoints | Missing server support | Implement `/users/{id}` and related aggregates or remove feature permanently |
| Search | Could require composite indexing | Define search scope and design (Postgres full-text / external service) |
| Calendar | Needs data model for adventures/events | Formalize adventure entity & scheduling semantics |

## Future Implementation Checklist
1. Design user/profile endpoints (read, partial update, delete).  
2. Define search specification (entities searched, pagination, ranking).  
3. Introduce adventure/event model for calendar (table + relationships).  
4. Implement ICS generation using `ics` library or custom renderer.  
5. UI: conditionally render stubbed feature buttons only when backend advertises capability (capabilities endpoint or config flag).  

## Verification Commands (Backend)
```
# List registered FastAPI routes (example introspection snippet)
python - <<'PY'
from backend_fastapi.app.main import app
for r in app.routes:
    if hasattr(r, 'methods'):
        print(sorted(r.methods), r.path)
PY
```

## Conclusion
Active user workflows are now mapped only to implemented endpoints. Legacy feature calls no longer generate failing network requests, reducing noise and user confusion. This establishes a stable baseline for iterative backend feature expansion.

---
Generated by automated audit process on 2025-11-13.
